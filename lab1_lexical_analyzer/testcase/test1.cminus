int main(void)
{
    /* This is a multiline comment.
     * And this is a start.
     * Then continue...
     * ......
     * Oh, we must end it up. */
    int cminus;
    int cplus;
    cminus = 10;
    cplus = 0;
    while (cminus > 0) {
        cminus = cminus - 1;
        cplus = cplus + 2;
    }
}
